class R{
  static int start = 0;
  static int notification = start++;
  static int storeNotAvailable = start++;
  static int cancelOrder = start++;
  static int reOrder = start++;
  static int home = start++;
  static int history = start++;
  static int account = start++;
}